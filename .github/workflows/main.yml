name: "My Githab Actions"
on: 
  push:
    branches:
      - main 
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello prod"
        run: echo "Hello, its prod!"
  prod_deployment:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello prod"
        run: echo "Hello, its prod!"
